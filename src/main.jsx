import React from 'react'
import ReactDOM from 'react-dom/client'
import EmailRoutingManager from '../email_routing_manager.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <EmailRoutingManager />
  </React.StrictMode>,
)
