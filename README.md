# ESP Server Manager

Система просмотра и управления серверами ESP и распределения потоков между клиентами.

## Возможности

- 📊 **Матрица маршрутизации** - настройка связок сервер-репутация-маска
- ⚙️ **Управление параметрами** - скорость (письма/мин), лимиты (письма/день), поточность
- 🔍 **Фильтрация и поиск** - по репутации, маскам, серверам
- 📱 **Адаптивный интерфейс** - работает на всех устройствах
- 🎨 **Современный UI** - интуитивно понятный интерфейс

## Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение откроется в браузере по адресу `http://localhost:3000`

### Сборка для продакшена

```bash
npm run build
```

### Предварительный просмотр сборки

```bash
npm run preview
```

## Структура проекта

```
system-flow/
├── src/
│   └── main.jsx          # Точка входа приложения
├── email_routing_manager.jsx  # Основной компонент
├── index.html            # HTML шаблон
├── package.json          # Зависимости и скрипты
├── vite.config.js        # Конфигурация Vite
└── README.md            # Документация
```

## Использование

1. **Просмотр по репутации** - группировка серверов по уровню репутации (Хорошая, Новая, Плохая)
2. **Просмотр по маскам** - группировка по почтовым провайдерам (gmail, яндекс, mail.ru, outlook, rambler)
3. **Просмотр по серверам** - все связки для конкретного сервера
4. **Фильтрация** - используйте фильтры для быстрого поиска нужных конфигураций
5. **Редактирование** - нажмите на иконку редактирования для изменения параметров сервера
6. **Назначение серверов** - используйте кнопку настроек для добавления/удаления серверов

## Технологии

- **React 18** - библиотека для создания пользовательских интерфейсов
- **Vite** - быстрый инструмент сборки
- **Tailwind CSS** - утилитарный CSS фреймворк
- **Lucide React** - набор иконок

## Требования

- Node.js 16+ 
- npm или yarn

## Развертывание на сервере

### Вариант 1: Vercel (Рекомендуется)

1. **Установите Vercel CLI:**
   ```bash
   npm install -g vercel
   ```

2. **Войдите в аккаунт Vercel:**
   ```bash
   vercel login
   ```

3. **Разверните проект:**
   ```bash
   vercel --prod
   ```

4. **Проект будет доступен по ссылке:** `https://your-project-name.vercel.app`

### Вариант 2: Netlify

1. **Установите Netlify CLI:**
   ```bash
   npm install -g netlify-cli
   ```

2. **Соберите проект:**
   ```bash
   npm run build
   ```

3. **Разверните:**
   ```bash
   netlify deploy --prod --dir=dist
   ```

### Вариант 3: Собственный сервер

1. **Соберите проект:**
   ```bash
   npm run build
   ```

2. **Скопируйте папку `dist` на сервер**

3. **Настройте Nginx/Apache для статических файлов**

## Обновление данных

После развертывания для обновления данных:

1. **Отредактируйте файлы:**
   - `queuesettings.txt` - конфигурация серверов
   - `тмп - Лист71 (1).csv` - маппинг очередей

2. **Пересоберите и разверните:**
   ```bash
   npm run build
   vercel --prod
   ```

3. **Или используйте кнопку "Обновить данные" в интерфейсе**
