#!/bin/bash

# Скрипт для быстрого развертывания проекта на сервере

set -e

echo "🚀 Начинаем развертывание ESP Server Manager..."

# Проверка Node.js
if ! command -v node &> /dev/null; then
    echo "❌ Node.js не установлен. Установите Node.js 16+ и повторите попытку."
    exit 1
fi

NODE_VERSION=$(node -v | cut -d'v' -f2 | cut -d'.' -f1)
if [ "$NODE_VERSION" -lt 16 ]; then
    echo "❌ Требуется Node.js версии 16 или выше. Текущая версия: $(node -v)"
    exit 1
fi

echo "✅ Node.js установлен: $(node -v)"

# Проверка npm
if ! command -v npm &> /dev/null; then
    echo "❌ npm не установлен."
    exit 1
fi

echo "✅ npm установлен: $(npm -v)"

# Установка зависимостей
echo "📦 Устанавливаем зависимости..."
npm install

# Сборка проекта
echo "🔨 Собираем проект..."
npm run build

# Создание директории для логов
echo "📁 Создаем директорию для логов..."
mkdir -p logs

# Проверка PM2
if ! command -v pm2 &> /dev/null; then
    echo "⚠️  PM2 не установлен. Устанавливаем..."
    sudo npm install -g pm2
else
    echo "✅ PM2 установлен: $(pm2 -v)"
fi

# Остановка старых процессов (если есть)
echo "🛑 Останавливаем старые процессы..."
pm2 stop ecosystem.config.js 2>/dev/null || true
pm2 delete ecosystem.config.js 2>/dev/null || true

# Запуск проекта
echo "▶️  Запускаем проект..."
pm2 start ecosystem.config.js

# Сохранение конфигурации PM2
echo "💾 Сохраняем конфигурацию PM2..."
pm2 save

# Вывод статуса
echo ""
echo "📊 Статус процессов:"
pm2 status

echo ""
echo "✅ Развертывание завершено!"
echo ""
echo "📝 Полезные команды:"
echo "  - Просмотр логов: pm2 logs"
echo "  - Статус: pm2 status"
echo "  - Перезапуск: pm2 restart all"
echo "  - Остановка: pm2 stop all"
echo ""
echo "🌐 Доступ к приложению:"
echo "  - Frontend: http://localhost:3000"
echo "  - Backend API: http://localhost:3001"
echo ""
echo "⚠️  Не забудьте настроить cron для автообновления данных!"
echo "   См. инструкцию в SERVER_DEPLOYMENT.md"

