# 📊 Управление данными Metabase BI

## 🎯 Обзор системы

Система автоматически кэширует данные из Metabase BI для быстрого доступа и анализа:

- **Первоначальная выгрузка**: Загружает данные за последние 30 дней
- **Ежедневные обновления**: Автоматически добавляет новые данные (вчера + сегодня)
- **Локальное кэширование**: Данные сохраняются в `cached_stats.csv`
- **Исторические данные**: Старые данные НЕ удаляются для анализа трендов

## 🚀 Быстрый старт

### 1. Первоначальная настройка

```bash
# Запустите прокси сервер
node proxy-server.js

# В другом терминале - первоначальная выгрузка
npm run data:init
```

### 2. Запуск приложения

```bash
npm run dev
```

## 📋 Команды управления данными

### Основные команды

```bash
# Первоначальная выгрузка за 30 дней
npm run data:init

# Ежедневное обновление (только новые данные)
npm run data:update

# Показать статистику данных
npm run data:stats

# Запустить автоматические обновления каждые 12 часов
npm run data:schedule
```

### Прямые команды

```bash
node manage-data.js init     # Первая настройка
node manage-data.js update   # Обновить данные
node manage-data.js stats    # Посмотреть статистику
node manage-data.js schedule # Автоматические обновления
```

## 📁 Структура файлов

```
system-flow/
├── cached_stats.csv      # Кэшированные данные из Metabase
├── data_config.json      # Конфигурация системы данных
├── data-manager.js       # Менеджер данных
├── manage-data.js        # CLI для управления данными
├── proxy-server.js       # Прокси для Metabase
└── email_routing_manager.jsx # Основное приложение
```

## 🔄 Как это работает

### Первоначальная выгрузка
1. **Загружает данные** за последние 30 дней из Metabase BI
2. **Сохраняет в CSV** файл `cached_stats.csv`
3. **Создает конфигурацию** в `data_config.json`

### Ежедневные обновления
1. **Загружает только новые данные** за последние 2 дня
2. **Объединяет с существующими** данными
3. **Удаляет дубликаты** (по дате + IdQueue + NameMailSender)
4. **Сохраняет обновленный файл**

### Автоматические обновления
- **Запускается каждые 12 часов** через cron
- **Работает в фоне** без вмешательства пользователя
- **Логирует результаты** в консоль

## 📊 Статистика данных

Команда `npm run data:stats` показывает:

```
📈 Статистика данных:
   Всего записей: 15,420
   Размер файла: 2,340 KB
   Последнее обновление: 2025-09-29T10:30:00.000Z
   Последняя полная выгрузка: 2025-09-28T09:00:00.000Z
   Диапазон данных: 2025-08-30 - 2025-09-29
```

## 🎛️ Настройка расписания

### Автоматические обновления

```bash
# Запустить планировщик (обновления каждые 12 часов)
npm run data:schedule

# Остановить: Ctrl+C
```

### Ручные обновления

```bash
# Обновить данные прямо сейчас
npm run data:update
```

## 🔧 Конфигурация

### Изменение частоты обновлений

В файле `manage-data.js` найдите строку:

```javascript
// Обновление каждые 12 часов
cron.schedule('0 */12 * * *', async () => {
```

Измените на нужную частоту:
- `0 */6 * * *` - каждые 6 часов
- `0 0 * * *` - каждый день в полночь
- `0 0 */2 * *` - каждые 2 дня

### Изменение периода данных

В файле `data-manager.js`:

```javascript
this.maxDays = 30; // Изменить на нужное количество дней
```

## 🚨 Устранение неполадок

### Ошибка "Файл данных не найден"

```bash
npm run data:init
```

### Ошибка подключения к Metabase

1. Убедитесь, что прокси сервер запущен:
   ```bash
   node proxy-server.js
   ```

2. Проверьте доступность Metabase BI

### Большой размер файла

Система сохраняет всю историю данных. Для очистки старых данных:

1. Откройте `cached_stats.csv`
2. Удалите строки старше нужной даты
3. Запустите `npm run data:stats` для обновления статистики

## 📈 Будущие улучшения

- **База данных**: Миграция с CSV на PostgreSQL/MySQL
- **API**: REST API для управления данными
- **Мониторинг**: Веб-интерфейс для мониторинга обновлений
- **Алерты**: Уведомления об ошибках обновлений
- **Аналитика**: Встроенные графики и тренды

## 💡 Рекомендации

1. **Регулярно проверяйте логи** обновлений
2. **Мониторьте размер файла** `cached_stats.csv`
3. **Делайте резервные копии** перед большими обновлениями
4. **Настройте автоматические обновления** для продакшена
