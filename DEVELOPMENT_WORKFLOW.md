# 🔄 Процесс разработки и деплоя

## 📋 Текущая структура

- **Локальная версия**: `http://localhost:3000` (для разработки и тестирования)
- **Продакшн версия**: `http://46.149.69.102` (доступна всем)

## 🛠️ Workflow разработки

### 1. Локальная разработка

```bash
# Запустите локальную версию
npm run dev:full

# Или отдельно:
npm run proxy    # Backend на порту 3001
npm run dev      # Frontend на порту 3000
```

**Доступ:**
- Frontend: http://localhost:3000
- Backend API: http://localhost:3001

### 2. Тестирование изменений

1. **Внесите изменения** в код
2. **Протестируйте локально** на `http://localhost:3000`
3. **Проверьте все функции**:
   - Загрузка данных
   - Обновление статистики
   - Фильтры
   - Все вкладки

### 3. Коммит и пуш изменений

```bash
# Добавьте изменения
git add .

# Закоммитьте с понятным сообщением
git commit -m "Описание изменений"

# Отправьте в GitHub
git push
```

### 4. Деплой на сервер

```bash
# На сервере выполните:
cd ~/system_flow
git pull
npm run build          # Пересоберите проект
./stop-server.sh       # Остановите старую версию
./start-server.sh      # Запустите новую версию
```

## 🔒 Система бекапов

### Автоматические бекапы

Перед каждым обновлением создается автоматический бекап через Git tags.

### Ручные бекапы

```bash
# Создать бекап текущей версии
./backup-server.sh

# Восстановить из бекапа
./restore-backup.sh backup-YYYY-MM-DD-HH-MM-SS
```

### Git Tags для версий

```bash
# Создать тег стабильной версии
git tag -a v1.0.0 -m "Стабильная версия"
git push origin v1.0.0

# Откатиться к стабильной версии
git checkout v1.0.0
```

## ⚠️ Важные правила

1. **Всегда тестируйте локально** перед деплоем
2. **Коммитьте часто** с понятными сообщениями
3. **Создавайте бекапы** перед большими изменениями
4. **Используйте Git tags** для стабильных версий
5. **Проверяйте логи** после деплоя

## 📝 Чеклист перед деплоем

- [ ] Изменения протестированы локально
- [ ] Все функции работают
- [ ] Код закоммичен и запушен
- [ ] Создан бекап (если большие изменения)
- [ ] Готов к деплою на сервер

## 🚨 Откат к предыдущей версии

Если что-то пошло не так:

```bash
# На сервере:
cd ~/system_flow

# Вариант 1: Откат через Git
git log --oneline          # Посмотреть историю
git checkout <commit-hash> # Откатиться к коммиту
npm run build
./stop-server.sh && ./start-server.sh

# Вариант 2: Откат через тег
git checkout v1.0.0        # Откатиться к тегу
npm run build
./stop-server.sh && ./start-server.sh

# Вариант 3: Восстановить из бекапа
./restore-backup.sh backup-YYYY-MM-DD-HH-MM-SS
```

